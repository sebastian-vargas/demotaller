
export interface Workshop {
    id_workshop?: Number;
    title: string;
    description: string;
    created_at?: Date;
    started_at?: Date,
    lessons_count?: Number,
    lessons_readed_count?: Number;
}